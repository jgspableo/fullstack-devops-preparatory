<h1><%= title %></h1>
<ul>
  <% if (users && users.length > 0) { %> <% users.forEach(user => { %>
  <li><%= user.username %></li>
  <% }) %> <% } else { %>
  <p>No users found.</p>
  <% } %>
</ul>
<a href="/">
  <button>Back to home</button>
</a>
